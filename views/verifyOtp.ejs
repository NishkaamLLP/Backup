<!doctype html>
<html>
  <!-- head -->
  <%- include('./partials/head.ejs', { title: 'Verify the OTP'}) %>
  <body>
    <section
      style="background-image: url('/images/products.png')"
      class="bg-gray-50 text-center dark:bg-gray-900"
    >
      <div
        class="flex flex-col items-center justify-center px-6 py-8 mx-auto md:h-screen lg:py-0"
      >
        <div
          class="w-full bg-gray-100 rounded-lg shadow dark:border md:mt-0 sm:max-w-md xl:p-0 dark:bg-gray-800 dark:border-gray-700"
        >
          <div class="p-6 space-y-4 md:space-y-6 sm:p-8">
            <h1
              class="text-xl font-bold leading-tight tracking-tight text-gray-900 md:text-2xl dark:text-white"
            >
              NISHKAAM INNOVATIONS - OTP VERIFICATION
            </h1>
            <form
              class="space-y-4 md:space-y-6"
              method="POST"
              action="/verifyOtp"
            >
              <div>
                <label
                  for="otp"
                  class="block text-left mb-2 text-sm font-medium text-gray-900 dark:text-white"
                  >OTP</label
                >
                <input
                  type="number"
                  name="otp"
                  id="otp"
                  class="bg-gray-50 border border-gray-300 text-gray-900 sm:text-sm rounded-lg focus:ring-primary-600 focus:border-primary-600 block w-full p-2.5 dark:bg-gray-700 dark:border-gray-600 dark:placeholder-gray-400 dark:text-white dark:focus:ring-blue-500 dark:focus:border-blue-500"
                  placeholder="Enter OTP here"
                  required=""
                />
              </div>

              <div class="flex items-center justify-between">
                <div class="flex items-start">
                  <!-- hidden input  -->
                  <input type="hidden" name="email" value="<%= token %>" />

                  <p for="remember" class="text-gray-500 dark:text-gray-300">
                    <a href="/resend">Resend Otp</a>
                  </p>
                </div>
              </div>
              <button
                type="submit"
                style="
                  background-color: blue;
                  color: white;
                  padding: 10px 20px;
                  border: none;
                  border-radius: 5px;
                  cursor: pointer;
                  margin-top: 10px;
                  width: 100%;
                  font-size: large;
                "
              >
                Verify
              </button>
            </form>
          </div>
        </div>
      </div>
    </section>
  </body>
</html>
